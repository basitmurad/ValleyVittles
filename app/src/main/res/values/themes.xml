<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.ValleyVittles" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textColorPrimary">@android:color/white</item>


        <!-- You can also customize other attributes here if needed -->
    </style>

    <style name="Theme.ValleyVittles" parent="Base.Theme.ValleyVittles" />

    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">
        <!-- Set the text color for the tab items -->
        <item name="tabTextColor">@android:color/white</item>
    </style>

    <style name="Theme.textsize" parent="Base.Theme.ValleyVittles" >
        <item name="android:text">24dp</item>
        <item name="editTextStyle">@font/roboto_bold</item>
        <item name="android:textColor">@color/white</item>
    </style>





</resources>
